[package]
name = "door-frost-assistant"
version = "0.1.0"
description = "A Tauri desktop shell for the frost assistant prototype."
authors = ["door maintainers"]
edition = "2021"
default-run = "door-frost-assistant"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde_json = "1"

[features]
default = []
